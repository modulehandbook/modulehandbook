<%= form_with(model: course, local: true, url: @course[:id] ? course_path(@course[:id]) : nil) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
        <% course.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <% unless program.nil? %>
  <h4>
  for Program: <%= program.select_name %>
  </h4>
  <%= hidden_field_tag(:program_id, program.id) %>
  <% end %>


  <%= render 'course_template', course: @course, editing: true, form: form %>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
